Timestamp: 2026-03-14
Prompt Number: Prompt 2.1

What changed
- Added `_infer_model_device`, `_move_batch_to_device`, and `_sanitize_generation_kwargs` inside the real `TransformersVisionLanguageBackend`
- Updated the real `generate()` path to move all tensor inputs onto the inferred model device before generation
- Updated the same path to drop ignored sampling-only kwargs when `do_sample=false`
- Added Prompt 2.1 lightweight tests and acceptance coverage
- Updated README with a short runtime-hygiene note

Why it changed
- Prompt 2.0 proved the unified real checkpoint-eval path worked structurally, but the runtime still emitted two avoidable warnings: CPU-vs-CUDA input placement and ignored deterministic-generation kwargs
- Prompt 2.1 closes only that runtime-cleanliness gap without touching trainer, SFT export, trajectory generation, reward logic, or unrelated phases

Effects
- Real checkpoint evaluation keeps the same single-entrypoint architecture and provenance outputs
- Multimodal processor tensors are moved to the model device before `model.generate(...)`
- Deterministic generation no longer passes ignored kwargs such as `temperature`, `top_p`, or `top_k`
- Local validation remains lightweight and does not run any heavy inference or training

Git-diff-style summary
- `src/agentiad_recon/backends.py`: added runtime device/generation hygiene helpers and routed `generate()` through them
- `tests/test_prompt_2_1_runtime_hygiene.py`: added helper and generate-path unit tests
- `audit/check_prompt_2_1.py`: added acceptance checks for helper presence and Prompt 2.0/2.1 test pass
- `README.md`: documented backend-side device placement and deterministic generation sanitation
