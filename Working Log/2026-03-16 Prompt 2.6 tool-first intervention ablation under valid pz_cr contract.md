Timestamp
- 2026-03-16 Asia/Shanghai

Prompt Number
- Prompt 2.6

What Changed
- Added a first-turn `pz_cr` prompt intervention layer with three auditable strategies: `baseline`, `tool_first_nudge`, and `tool_first_strict`.
- Wired the selected strategy through unified `baseline.py` runtime/config/CLI surfaces and into runtime provenance, prompt-audit sidecars, metrics, run summary, and run manifest.
- Added a per-run `tool_first_strategy_summary.json` artifact so baseline/nudge/strict runs can be compared without creating a second evaluator.
- Added Prompt 2.6 tests and `audit/check_prompt_2_6.py` to verify strategy selection, sidecar differences, provenance, and that no strategy path fabricates tool calls.

Why It Changed
- Prompt 2.4 smoke proved the `pz_cr` prompt surface is valid.
- Prompt 2.5 showed `toolcall_rate` is still `0.0`, so the blocker is now behavioral zero-tool collapse under a valid runtime contract.
- This patch does not claim that the model is fixed. It only adds the minimal, auditable intervention ablation needed to test whether stronger first-turn tool-first prompting changes behavior.

Effects
- The unified evaluator can now run the same checkpoint under `baseline`, `tool_first_nudge`, or `tool_first_strict` first-turn prompting while preserving the same parser, tool loop, artifacts, and summary contracts.
- First-turn prompt-audit sidecars now show which strategy was used, whether intervention text was applied, and a digest of the rendered prompt surface.
- Each run emits a comparable strategy summary artifact alongside the existing zero-tool behavior summary.

Git-Diff-Style Summary
- `src/agentiad_recon/prompting.py`: added `pz_cr`-only first-turn intervention text for `tool_first_nudge` and `tool_first_strict`
- `src/agentiad_recon/baseline.py`: threaded strategy through runtime config/CLI, prompt audit, provenance, and run artifacts
- `src/agentiad_recon/behavior_audit.py`: added per-strategy summary artifact writer
- `tests/test_prompt_2_6_tool_first_intervention_ablation.py`: added lightweight strategy/provenance/sidecar regression tests
- `audit/check_prompt_2_6.py`: added auto-discovering acceptance with scored pass/fail output

Local Validation
- `python -m compileall src tests audit`
- `python -m unittest discover -s tests`
- `python audit/check_prompt_2_6.py`

Notes
- No trainer logic changed.
- No SFT export semantics changed.
- No canonical trajectories changed.
- No output rewriting or synthetic tool-call fabrication was introduced.
