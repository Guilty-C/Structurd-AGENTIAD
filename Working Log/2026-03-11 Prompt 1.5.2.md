# 2026-03-11 Prompt 1.5.2

- timestamp: 2026-03-11 13:45
- prompt number: 1.5.2

## What Changed

- Re-verified Prompt 1.5 thin adapter/export path against current code and remote templates.
- Re-generated the remote-template MS-Swift dataset locally (fixture-backed, lightweight) and produced:
  - `agentiad_prompt_1_5_remote_ms_swift_dataset.jsonl`
  - `agentiad_prompt_1_5_remote_ms_swift_dataset.manifest.json`
- Added explicit anti-regression checks to enforce `images` entries are non-empty string paths (not dict structures like `{"bytes": ..., "path": ...}`):
  - `tests/test_prompt_1_5_sft_export.py`
  - `audit/check_prompt_1_5.py`

## Why It Changed

- The reported runtime evidence showed historical failures where exported records still used old multimodal list content and dict-style image entries.
- Prompt 1.5.2 acceptance requires proof on the re-exported JSONL artifact itself, not only schema/validator code.
- Adding explicit tests for `images: list[str]` prevents silent regression back to dict-image structures.

## Effects

- Re-exported remote-template dataset now confirms:
  - `messages[*].content` is string-only
  - `images` is `list[str]` only
  - total `<image>` count equals `len(images)` per record
  - `images` order matches first-occurrence order from canonical message image refs
- Prompt 1.5 acceptance checks and unit tests now explicitly fail if dict-style image entries reappear.

## Git-Diff-Style Summary

- `M` `tests/test_prompt_1_5_sft_export.py` (explicit `images` type assertions)
- `M` `audit/check_prompt_1_5.py` (explicit `images` string-path checks)
- `A` `Working Log/2026-03-11 Prompt 1.5.2.md`
