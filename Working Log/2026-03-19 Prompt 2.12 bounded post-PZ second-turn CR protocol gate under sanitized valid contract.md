# Prompt 2.12 Working Log

## What Changed

- Added an opt-in `post_pz_second_turn_gate_mode` runtime option to [baseline.py](/home/zbr/project/lrrelevant/Structurd-AGENTIAD/src/agentiad_recon/baseline.py) with `off` and `retry_once_require_cr_after_pz`.
- Added a bounded post-PZ second-turn CR protocol gate that triggers only when the run is `pz_cr`, the first successful canonical tool is `PZ`, the cleaned post-PZ transition contract is valid for `CR`, and the first assistant turn after `PZ` still returns a direct final answer.
- Preserved the original second-turn raw output, appended one deterministic runtime reminder, retried exactly once, and recorded explicit gate outcomes including recovery to `CR`, retry parse failure, still-terminal-after-retry, non-`CR` tool after retry, and `CR` call followed by later execution/contract failure.
- Added gate sidecars, prediction-level fields, run summaries, metrics/report schema coverage, and a compact `post_pz_second_turn_gate_summary` artifact. 

## Why

- Prompt 2.11 showed that the main blocker after `PZ` was no longer stale `pz_only` leakage; the active second-turn surface could be cleaned and valid for `CR`.
- The next scientific question was whether one bounded deterministic reminder can recover any real `CR` calls when the model still direct-final-answers on that already-clean valid contract.
- This patch answers that question without fabricating `CR`, without rewriting outputs, and without widening parser permissiveness outside the existing first-turn repair lane.

## Effects

- `baseline.py` remains the single evaluation entrypoint while now supporting an auditable opt-in post-PZ second-turn gate.
- Raw evidence is preserved for both the first post-PZ second-turn output and the retry output.
- Remote smoke/full runs can now measure whether a single deterministic reminder recovers real `CR` behavior, versus producing parse failures, wrong-tool calls, or repeated terminal answers.
- Existing Prompt 2.11 sanitation evidence remains intact and is now directly connected to second-turn gate outcome summaries.
