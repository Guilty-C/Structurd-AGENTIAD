# 2026-03-10 Prompt 1.5

- timestamp: 2026-03-10
- prompt number: 1.5

## What Changed

- Added `src/agentiad_recon/sft.py` as the canonical Prompt 1.5 exporter for unified `pz_only` plus `pz_cr` SFT trajectories.
- Added Prompt 1.5 SFT schemas for export definitions, dataset rows, manifests, MS-Swift recipes, and MS-Swift-facing records.
- Added `src/agentiad_recon/ms_swift_adapter.py` as a thin adapter layer that validates recipes, projects canonical records into an MS-Swift-facing JSONL shape, and reports runtime availability honestly.
- Added Prompt 1.5 configs for local SFT export, local MS-Swift recipe validation, and remote-only full-SFT template preparation.
- Added Prompt 1.5 tests, acceptance gate, README/config/contract docs, and acceptance reporting.

## Why It Changed

- The master checklist requires the next milestone after tool-enabled inference to be auditable SFT trajectory reconstruction plus thin framework wiring.
- The repo already had one canonical prompt/tool/trace waist, so the SFT layer needed to extend that same path instead of creating a parallel training pipeline.
- The prompt explicitly required local-only export and validation while keeping heavy SFT execution deferred to the remote SSH server.

## Effects

- The repo can now export deterministic `pz_only` and `pz_cr` trajectories into one schema-validated SFT dataset with explicit tool events, image bindings, exemplar linkage, and decisive-turn loss masks.
- The repo now emits a thin MS-Swift-facing dataset projection and externalized recipe/config surfaces for LoRA, checkpoints, logging, resume, and dataset path plumbing.
- Local sanity checks now cover dataset hashes, message ordering, image and exemplar bindings, decisive-turn masking, and adapter/runtime honesty without running full training.

## Git-Diff-Style Summary

- `A` `src/agentiad_recon/sft.py` for canonical Prompt 1.5 trajectory export and local validation
- `A` `src/agentiad_recon/ms_swift_adapter.py` for thin MS-Swift recipe validation and runtime probing
- `A` Prompt 1.5 contract schemas under `src/agentiad_recon/contracts/schemas/`
- `A` `configs/sft_export_fixture.json`, `configs/ms_swift_sft_fixture.json`, and `configs/ms_swift_sft_remote_template.json`
- `A` `tests/test_prompt_1_5_sft_export.py`
- `A` `audit/check_prompt_1_5.py`
- `M` README, config docs, contract docs, adapter docs, and package exports for Prompt 1.5
