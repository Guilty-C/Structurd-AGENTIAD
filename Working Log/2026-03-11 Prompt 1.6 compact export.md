# 2026-03-11 Prompt 1.6 compact export

- timestamp: 2026-03-11 13:45
- prompt number: 1.6

## What Changed

- Patched only the thin MS-Swift-facing rendering in `src/agentiad_recon/sft.py` to reduce dialogue inflation while preserving canonical trajectory records.
- Added compact tool-request rendering: assistant tool-request messages now keep a compact `<tool_call>{...}</tool_call>` block instead of verbose duplicated prose.
- Added compact tool-result rendering: training text now keeps only compact tool-relevant fields (`tool`, compact `bbox` / exemplar cues, `<image>` cue) and drops verbose artifact metadata from dialogue text.
- Added duplicate-prose dedupe for `<think>` patterns in compact rendering.
- Added lightweight compact-export length audit sidecar with percentile stats and top offending records.
- Extended swift manifest with compact length-audit summary pointers.
- Updated Prompt 1.5 tests and acceptance script to check compactness-specific constraints and length-audit existence.

## Why It Changed

- Current SFT-facing export passed schema but remained too verbose for practical max-length/VRAM constraints.
- Failures moved from format incompatibility to sequence-length pressure and training memory pressure.
- The correct fix is adapter-layer compaction, not trainer or framework internal changes.

## Effects

- Canonical/audit trajectories remain intact for traceability and contract checks.
- MS-Swift-facing export is now materially more compact while preserving message order, image bindings, trajectory split semantics, final-answer supervision, and decisive-turn masking intent.
- Export now emits machine-readable compact length statistics to guide remote max-length choice before heavy runs.

## Git-Diff-Style Summary

- `M` `src/agentiad_recon/sft.py`
- `M` `src/agentiad_recon/contracts/schemas/sft_dataset_manifest.schema.json`
- `M` `tests/test_prompt_1_5_sft_export.py`
- `M` `audit/check_prompt_1_5.py`
- `M` `README.md`
- `A` `Working Log/2026-03-11 Prompt 1.6 compact export.md`
