# Evaluation Scope

Evaluation code in this reconstruction should begin with sample-level local validation only. Dataset-wide evaluation, benchmark sweeps, and remote compute-heavy analysis are intentionally deferred until after the contracts and adapters are implemented.
